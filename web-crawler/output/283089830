电脑/网络 编程语言 C/C++ 
关于C语言指针的问题以及malloc分配存储单元之后为什么可以用数组对存储单元进行操作？。。。
<br /> { &#47：为什么InitTriplet中用 (*T)[0]代表存储单元的值;<br /> }<br /><br />请高人回答.h 采用动态分配的顺序存储结构 *&#47;*这一句不明白，而在ISAscending中又用T[O]代表存储单元的值，用malloc分配存储空间之后？<br />问题2？*&#47：<br /><br />问题1。操作结果; &#47;=T[2])，这里又可以用T[0]来代表存储单元的值;* 由InitTriplet分配3个元素存储空间 *&#47,ElemType v3) &#47：三元组T已存在，那么*T=(ElemType *)malloc(3*sizeof(ElemType))，然后再给出函数;<br />   return(T[0]&lt：在InitTriplet函数中。，为什么是数组？<br />问题3;*这里也不明白;* Triplet类型是ElemType类型的指针;&#47;&#47，依次置T的3个元素的初值为v1;* 操作结果;<br /> &#47;<br /><br />先提出问题,(*T)[2]=v3；(*T)[0]为什么就是存储单元的值，否则返回0 *&#47;&#47：<br />Status InitTriplet(Triplet *T？<br />   if(;=T[1]&&T[1]&lt;<br />   (*T)[0]=v1;<br />   return OK，存放ElemType类型的地址 *&#47,(*T)[1]=v2？<br /><br />部分函数如下：构造三元组T,ElemType v2，返回1&#47，返回一个指针类型。，下面那个函数里的T[0]为什么不用加*号：我想知道InitTriplet为什么要用Triplet *T;<br /> }<br /><br />Status IsAscending(Triplet T)<br /> { &#47;&#47？*&#47，为什么不是T[0];* c1-1;* 初始条件，为什么前面 InitTriplet函数要用(*T)[0]来代表存储单元的值，v2和v3 *&#47：如果T的3个元素按升序排列;<br /> typedef ElemType *Triplet;为什么要加一个*号!*T)<br />     exit(OVERFLOW),ElemType v1，Triplet不已经就是一个指向整型数据的指针吗;<br />   *T=(ElemType *)malloc(3*sizeof(ElemType))，而且要加*号
总之这里所有跟你所想不同的特性都是由<br />Triplet *T这条语句造成的,(*T)[1]=v2;这句？你知道吗，T不是你预想的指向ElemType的指针，T的定义是ElemType *T;这一句声明了Triplet是一个指向ElemType的指针类型;/*这一句不明白;这条语句中T前面为什么要加*,即T是一个指向ElemType的指针的指针：<br />问题3？<br />因为Status IsAscending(Triplet T)这表明T是一个指向ElemType的指针，下面那个函数里的T[0]为什么不用加*号，而且要加*号，而在另一个函数里？<br />根本原因就是 在InitTriplet函数里？<br />这里是为什么呢;* (*T)[0]=v1，根据 *T=(ElemType *)malloc(3*sizeof(ElemType));这是为什么呢，T的定义是ElemType **T;对于该问题，而是指向ElemType的指针的指针，根据上下文环境我猜测你上面还有一句typedef int ElemType，这里又可以用T[0]来代表存储单元的值：为什么InitTriplet中用 (*T)[0]代表存储单元的值？*&#47，这里定义实际与ElemType **T等价，以下的疑问相信你能够推知了吧<br />对于为什么前面 InitTriplet函数要用(*T)[0]来代表存储单元的值，Triplet T可以等价为ElemType* T，为什么是数组：<br />问题1，而在ISAscending中又用T[O]代表存储单元的值？<br />InitTriplet为什么要用Triplet *T。<br /><br />对于问题3：我想知道InitTriplet为什么要用Triplet *T，再C语言里对对于这种引用方式(*T)[0]实际上是被解析为*((*T)+0)，Triplet不已经就是一个指向整型数据的指针吗，(*T)实际上是指向动态分配的内存的首地址,(*T)[2]=v3。&#47，这就可以解释为什么 *T=(ElemType *)malloc(3*sizeof(ElemType))，typedef ElemType *Triplet首先回答你的第一个问题
3
none
http://zhidao.baidu.com/question/207240089.html?qbl=relate_question_0
http://zhidao.baidu.com/question/231892335.html?qbl=relate_question_1
http://zhidao.baidu.com/question/18519177.html?qbl=relate_question_2
http://zhidao.baidu.com/question/493961597.html?qbl=relate_question_3
http://zhidao.baidu.com/question/131627761.html?qbl=relate_question_4
